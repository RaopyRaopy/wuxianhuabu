---
title: Docker 部署
description: 使用 Docker Compose 部署视频创作台
---

# Docker 部署

## 构建并启动

```bash
git clone git@github.com:basketikun/infinite-canvas.git
cd infinite-canvas
docker compose up -d --build
```

访问 `http://localhost:3000`。

## 本地镜像

```bash
docker compose -f docker-compose.local.yml up -d --build
```

## 数据说明

Docker 镜像只托管 Vite 构建出的静态前端文件，并通过 nginx 提供访问。视频生成记录、参考素材和 AI API Key 默认保存在访问者浏览器本地；需要同步时，请在应用设置中配置 WebDAV。
